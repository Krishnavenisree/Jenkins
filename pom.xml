<profiles>
    <profile>
        <id>matrix</id>
        <activation>
            <activeByDefault>false</activeByDefault>
        </activation>
        <properties>
            <!-- Define any custom properties you need -->
        </properties>
        <!-- You can also define build settings, dependencies, plugins, etc. -->
    </profile>
</profiles>